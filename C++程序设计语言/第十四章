1.从名字空间外引用成员的方式包括：using声明，using指示，参数依赖查找

2.重载一个名字时，using声明会应用于所有重咋版本 

3.查找函数f()的声明时，并不优先选择所在namespace中的定义（而是依据重载解析规则），对于一个类中的f()调用，则会优先查找同一个类中的声明（14.2.4）

4.using声明将名字添加到局部作用域（隐藏外层变量），using指示令名字在其所在的作用域可见（与外层同名变量冲突，未用名字的冲突不认为是错误）

5.namespace A=B  A是名字空间B的别名（用于简化名字或者版本库替换14.4.2）

6.函数重载是跨越名字空间的（不同空间的同名函数可以相互重载）

7.每个无名名字空间都有一个隐含的using指示   