1.函数调用自动执行 参数隐式类型转换 和 返回值隐式类型转化

2.内联函数和内联函数中的static变量拥有唯一的地址（inline不影响函数的语义）

3.constexpr出现在函数定义表示：若参数为常量表达式，则该函数能用于常量表达式
  constexpr出现在对象定义中表示：在编译时对初始化器求值

4.constexpr函数必须足够简单，没有 if ，循环，局部变量，没有副作用（比如修改全局变量），必须包含一条独立的return语句
  constexpr函数允许递归和条件表达式
  constexpr函数可以接受const引用实参，允许返回指针或者引用

5. constexpr构造函数 见10.4.3

6.[[...]]称为属性(attribute)，C++包含两种标准属性  [[noreturn]]和[[carries_dependency]]

7.static局部变量在线程第一次到达它的定义时被初始化，递归初始化局部static变量是未定义行为

8.可变数量参数三种方式：使用可变模板，initializer_list ， 使用...结束参数列表（使用<cstdarg>中的宏处理参数）

9.解引用函数指针可以用*也可以不用，获取函数地址可以用&也可以不用