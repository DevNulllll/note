1.定义模板时检测语法相关错误（类型检查），使用模板时检测依赖于模板参数的错误（错误最早发现于模板第一次使用时，称为实例化点）

2.一个类有多个构造函数，导致类模板参数不能靠推断确定

3.类模板的非静态数据成员不能是constexpr的

4.模板参数类型（T）只能模板内部使用，其他代码使用类型T需要在模板内部定义类型T的别名
  例如 using value_type=T;
  如果一个类漏掉了需要的成员别名，可以用类型萃取机制弥补



5.模板函数和普通函数同时匹配，优先选择普通函数