def语句在运行时计算，def中的代码在函数被调用后才会计算


全局作用与仅限于单个文件，Python中没有跨文件的全局作用域


全局变量在函数内部如果被赋值，必须有global声明（如果外围模块中不存在该变量，global声明自动在模块中创建该变量）
全局变量在函数内部不经过声明也可以引用（不能赋值）


Python迭代上下文工具遍历序列的两种方式：
        1.对于支持迭代协议的对象，调用__next__方法直至返回异常
        2.对于不支持迭代协议的对象，使用索引协议，从0至len