IPV6地址表示规则：
    （1）16位块的前导0必须压缩  
    （2）：：只能用在影响最大的地方（压缩最多的0），相同长度则靠前的优先
    （3）a到f的十六进制数字用小写表示


广播地址：掩码取反 与网络中任意地址做或运算  （定向广播，区别于本地广播）


CIDR：任何未经预定义（指A，B，C类网络）的地址范围可作为一个类的一部分
聚合：采用分层路由思想，每个路由器拥有个路由表条目数不超过它的链路数目
路由聚合：多个相邻的IP路由前缀合并为一个短前缀（多个路由条目被聚合成一个）
