p18

对于C—string，两参数版本按值传递，但是三参数版本按引用传递，并且返回引用，
所以最终三参数版本返回时，返回的是对两参数版本中形参指针的引用，即临时量的引用，
所以会出现问题，关键在于两种声明不统一，如果都是引用或者都是按值传递，则不会有问题


调用max(7,42,68)时，虽然也会生成临时量，但此时的临时量是在进入三参数版本之前，即在main中生成
的匿名临时量，所以最终返回对临时量的引用不会出现问题，声明对c-String的调用，临时指针是在三参数版本内部生成的


