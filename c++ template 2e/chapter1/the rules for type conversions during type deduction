P8


类型推导时仅支持有限的自动类型转换

1. 当模板函数的形参被声明为引用时，不支持任何类型转换，同一个模板参数在不同形参中的推导必须匹配
2. 当形参被声明为值时，支持decay的转换
    const和volatile修饰符被忽略(这里的const指顶层const？)
    引用转换为被引用的类型
    数组和函数被转换为指针
    同一模板参数在不同形参中的推导结果，其decay后的类型必须匹配（即可以decay到同一类型）

        
