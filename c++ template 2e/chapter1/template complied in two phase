P6


1.在还没有实例化的模板定义阶段
    检查语法错误
    检查不依赖于模板参数的名称引用（类型名，函数名等）
    不依赖于模板参数的静态断言
    (某些编译器进行不完全的第一次检查，所以有些错误知道第一次实例化时才报告，例如vs2013,vs2015)

2.实例化阶段
    对所有依赖于模板参数的部分进行第二次检查


以上内容术语 two-phase lookup
两阶段检查使模板函数在实例化时，编译器需要看到函数的定义，而普通的函数调用时，编译器只需要看到函数声明即可


