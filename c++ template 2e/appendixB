P673

c语言标准化之前，左右值以能否出现在赋值符号左侧区分
c语言标准化之后，并非所有左值都能出现在赋值符左侧，例如 const int x;
c++中甚至右值也能出现在赋值符左侧，此时代表调用右值对象的赋值运算符而不是单纯的赋值含义
因此区分左右值按该值是否与某个内存地址关联来判断，左值可以引用其地址，右值只参与计算

字符串字面值也是左值（不可修改）


decltype((x))
type if x is a prvalue
type& if x is a lvalue
type&& if x is a xvalue


